<h1>Listing icons</h1>

<table>
<% @icons.each do |icon| %>
  <tr>
    <td><img src="<%= icon_path icon %>"/><%= link_to '&thinsp; '.html_safe, icon, confirm: 'Are you sure?', method: :delete,:class=>"apice" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Icon', new_icon_path %>
